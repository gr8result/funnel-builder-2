import type { ApprovalInput } from "../services/approvalStageService";

export function ClientApprovalActions({ input, onChange, onPrepare, onSent, onReviewing, onChanges, onApprove, onRevoke }: { input: ApprovalInput; onChange: (input: ApprovalInput) => void; onPrepare: () => void; onSent: () => void; onReviewing: () => void; onChanges: () => void; onApprove: () => void; onRevoke: () => void }) {
  return <section className="approvalCard"><h2>Client Approval Actions</h2><div className="approvalFields"><input value={input.approverName} onChange={(event) => onChange({ ...input, approverName: event.target.value })} placeholder="Approver name" /><input value={input.approverRole} onChange={(event) => onChange({ ...input, approverRole: event.target.value })} placeholder="Role or relationship" /><select value={input.method} onChange={(event) => onChange({ ...input, method: event.target.value as ApprovalInput["method"] })}><option value="in_app">In-App</option><option value="email_confirmation">Email Confirmation</option><option value="signed_document">Signed Document</option><option value="in_person">In Person</option><option value="phone_confirmation">Phone Confirmation</option><option value="other">Other</option></select><input value={input.declaration} onChange={(event) => onChange({ ...input, declaration: event.target.value })} placeholder="Declaration" /></div><div className="approvalButtons"><button type="button" onClick={onPrepare}>Prepare for Client Review</button><button type="button" onClick={onSent}>Mark as Sent</button><button type="button" onClick={onReviewing}>Record Client Reviewing</button><button type="button" onClick={onChanges}>Record Changes Requested</button><button type="button" className="primaryButton" onClick={onApprove}>Record Client Approval</button><button type="button" onClick={onRevoke}>Revoke Client Approval</button></div><p>Builder-entered approvals are recorded as representative entries, not cryptographic client signatures.</p></section>;
}
