import type { ApprovalInput } from "../services/approvalStageService";

export function BuilderApprovalActions({ input, onChange, onApprove, onRevoke }: { input: ApprovalInput; onChange: (input: ApprovalInput) => void; onApprove: () => void; onRevoke: () => void }) {
  return <section className="approvalCard"><h2>Builder Approval Actions</h2><div className="approvalFields"><input value={input.approverName} onChange={(event) => onChange({ ...input, approverName: event.target.value })} placeholder="Builder approver" /><input value={input.approverRole} onChange={(event) => onChange({ ...input, approverRole: event.target.value })} placeholder="Role" /><input value={input.declaration} onChange={(event) => onChange({ ...input, declaration: event.target.value })} placeholder="Declaration" /></div><div className="approvalButtons"><button type="button" className="primaryButton" onClick={onApprove}>Record Builder Approval</button><button type="button" onClick={onRevoke}>Revoke Builder Approval</button><button type="button">Add Internal Approval Note</button></div><p>Builder approval may be recorded before client approval in this implementation, but snapshot locking still requires both current approvals.</p></section>;
}
