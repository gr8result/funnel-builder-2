import type { EstimateMappingOverride } from "../repositories/documentsExportRepository";

export function EstimateMappingOverrideDialog({ lineId, value, onChange, onSave }: { lineId?: string; value: Partial<EstimateMappingOverride>; onChange: (value: Partial<EstimateMappingOverride>) => void; onSave: () => void }) {
  return <section className="documentsCard"><h2>Manual Mapping Override</h2><p>{lineId ? `Editing ${lineId}` : "Choose a mapping issue line before saving an override."}</p><div className="formGrid"><input placeholder="Estimate stage" value={value.estimateStage ?? ""} onChange={(event) => onChange({ ...value, estimateStage: event.target.value })} /><input placeholder="Estimate row mapping" value={value.estimateRowMapping ?? ""} onChange={(event) => onChange({ ...value, estimateRowMapping: event.target.value })} /><input placeholder="Cost code" value={value.costCode ?? ""} onChange={(event) => onChange({ ...value, costCode: event.target.value })} /><input placeholder="Trade mapping" value={value.tradeMapping ?? ""} onChange={(event) => onChange({ ...value, tradeMapping: event.target.value })} /><input placeholder="Unit" value={value.unit ?? ""} onChange={(event) => onChange({ ...value, unit: event.target.value })} /><input placeholder="Reason" value={value.reason ?? ""} onChange={(event) => onChange({ ...value, reason: event.target.value })} /></div><button type="button" disabled={!lineId} onClick={onSave}>Save Mapping Override</button></section>;
}
