{
  // Leave Deno installed, but do NOT let it take over the workspace
  "deno.enable": false,

  // Stop VSCode from screaming about the Supabase function .ts files
  "typescript.validate.enable": false,
  "javascript.validate.enable": true,

  // Treat this extensionless billing file as JSX to prevent false CSS parser errors
  "files.associations": {
    "**/pages/modules/billing/email-plans2": "javascriptreact"
  }
}
